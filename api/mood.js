export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({message:'Method not allowed'});try{const{mood}=req.body||{};const key=process.env.GEMINI_API_KEY;if(!key)return res.status(503).json({message:'Add GEMINI_API_KEY to enable Mood Matcher.'});if(typeof mood!=='string'||!mood.trim())return res.status(400).json({message:'Mood is required.'});const prompt='Return exactly one movie title matching this mood. No quotes, year, punctuation or explanation. Mood: '+mood.slice(0,500);const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key='+encodeURIComponent(key),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{temperature:.4,maxOutputTokens:30}})});if(!r.ok)throw new Error('Gemini request failed.');const d=await r.json(),title=d?.candidates?.[0]?.content?.parts?.[0]?.text?.trim();if(!title)throw new Error('No movie suggestion returned.');return res.status(200).json({title:title.replace(/^['"]|['"]$/g,'')})}catch(e){console.error(e);return res.status(500).json({message:'Mood matching is temporarily unavailable.'})}}