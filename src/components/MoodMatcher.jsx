import{useState}from'react';export default function MoodMatcher({onResult}){const[mood,setMood]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function go(){if(!mood.trim())return;setBusy(true);setError('');try{const r=await fetch('/api/mood',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mood:mood.trim()})});const d=await r.json();if(!r.ok)throw new Error(d.message);onResult(d.title)}catch(e){setError(e.message||'Mood matcher unavailable.')}finally{setBusy(false)}}return <section className='mood'><h2>AI Mood Matcher</h2><p>Describe a mood and we will search for one matching movie.</p><div><label className='sr' htmlFor='mood'>Mood</label><input id='mood' value={mood} onChange={e=>setMood(e.target.value)} placeholder='e.g. nostalgic and emotional'/><button onClick={go} disabled={!mood.trim()||busy}>{busy?'Matching...':'Match mood'}</button></div>{error&&<small className='error'>{error}</small>}</section>}