import{imageUrl}from"../api/tmdb.js";export default function MovieCard({movie,favorite,onToggle}){return <article className="card"><div className="poster">{movie.poster_path?<img src={imageUrl(movie.poster_path)} alt={movie.title+" poster"} loading="lazy"/>:<span>No poster</span>}<button className={"heart "+(favorite?"on":"")} onClick={()=>onToggle(movie)} aria-label={(favorite?"Remove ":"Add ")+movie.title+" "+(favorite?"from":"to")+" favorites"} aria-pressed={favorite}>{favorite?"♥":"♡"}</button></div><h2>{movie.title}</h2><div className="meta"><span>{movie.release_date?.slice(0,4)||"N/A"}</span><span>★ {movie.vote_average.toFixed(1)}</span></div></article>}